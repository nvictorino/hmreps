section.funding.content-section
  .highlight-right
    != PrismicDOM.RichText.asHtml(slice.primary.funding_narrative, ctx.linkResolver)
  
  .highlight-right
    img(src=slice.primary.funding_image.url)